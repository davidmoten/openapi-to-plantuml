openapi: 3.0.1
components:
  schemas:
    Customer:
      properties:
        firstName:
          type: string
        lastName:
          type: string
      required:
      - firstName
      - lastName
    Client:
      $ref: "#/components/schemas/Customer"
        