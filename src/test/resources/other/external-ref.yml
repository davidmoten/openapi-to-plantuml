openapi: 3.0.1
info:
  title: unit test
  version: 0.0.1
  
components:
  schemas:
    employee:
      properties:
        role: 
          type: string
          enum: ['DOCTOR','NURSE']
        agreement:
          $ref: 'ref.yml#/components/schemas/agreement'
      required:
      - role
      - agreement